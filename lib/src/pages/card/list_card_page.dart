import 'package:flutter/material.dart';

class ListCardPage extends StatelessWidget {
  const ListCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Список карт'));
  }
}
